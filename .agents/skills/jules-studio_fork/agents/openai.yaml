interface:
  display_name: "Jules Studio Fork"
  short_description: "Repo-specific patterns and workflows for jules-studio_fork"
  default_prompt: "Use the jules-studio_fork repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true